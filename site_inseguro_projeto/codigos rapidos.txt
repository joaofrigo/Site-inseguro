cd "C:\Users\joaof\Desktop\site inseguro\Site-inseguro"
Set-ExecutionPolicy RemoteSigned -Scope Process
virtual\Scripts\activate

cd site_inseguro_projeto
python manage.py runserver

cd C:\Users\joaof\Desktop\site inseguro\Site-inseguro
git commit -m

o comando só pode ser utilizado em terminais que aceitam curl, como o git bash, ou linux no geral
curl -H "X-Forwarded-For: 189.72.57.45" http://127.0.0.1:8000/
O forwarded emite um ip de mentirinha pro servidor que daí pega a informação

O plot twist do porque a senha de input no facebook não ia, era por causa que a senha ia para o "encpass" que criptografava a senha antes. continuei com o input existindo, hidden nele e coloquei outro em cima

Preciso achar uma forma de criar um dicionario big que tenha todas as requisições salvas nele, possivelmente evitando redundâncias. Agora só falta se eu quiser, tirar as redundâncias de valores da lista iguais.

O plot twist é que o forms manda para uma url um request e processamos esse request na view daí.

Aplicar clickjacking e ver se posso adicionar mais informações adquiridas. Possivelmente através
da leitura de coisas do próprio wifi adicionais. input instagram começa depois de 73

Etapas para configurar um html direito phising:

Para conseguir copiar todo uma pagina no html, só preciso apertar com botão direito no html e "edit as html" depois ctrl a

Depois arrumar views e urls. Uma view para entrar e uma view para processar o post.

Arrumamos os links quebrados e retiramos partes irrelevantes

Depois achamos o input, alteramos o form para mandar o input para a url que processa o post e é isso.